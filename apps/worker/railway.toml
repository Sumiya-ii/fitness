[build]
builder = "nixpacks"
buildCommand = "npm ci && npm run build --workspace=packages/shared && npm run build --workspace=apps/worker"

[deploy]
startCommand = "npm run start --workspace=apps/worker"
restartPolicyType = "on_failure"
numReplicas = 1
