# Sentry Report — 2026-05-24

## Summary
- 0 new errors in the last 24 hours
- 2 existing unresolved issues (both pre-date today; carried over from prior report)
- 0 fixed today, 1 needs human review (Railway env var)

## No New Errors

No new unresolved errors in the last 24 hours.

---

## Existing Issues (not new today)

### Previously Fixed
- **error: column p.goal_type does not exist** (node / coach-memory worker)
  - Sentry ID: 7459412311 (NODE-6)
  - Fixed: 2026-05-23 in commit `581b718` — changed `p.goal_type` to `t.goal_type` in the `deriveFacts` SQL query. The `goal_type` column lives on the `targets` table (alias `t`), not `profiles` (alias `p`).
  - Last seen: 2026-05-04. No recurrence since fix deployed.
  - Files changed: `apps/worker/src/processors/coach-memory.processor.ts`

## Needs Review

- **Error: LINK_CODE_SECRET environment variable is required** (node / API)
  - Sentry ID: 7445552823 (NODE-5)
  - Last seen: 2026-04-27 (7 occurrences)
  - Culprit: `TelegramService.hashLinkCode` → `POST /api/v1/telegram/link-code`
  - Root cause: `LINK_CODE_SECRET` env var is not set in the Railway API service. The code (`apps/api/src/telegram/telegram.service.ts:32`) correctly validates its presence and throws; the fix is **not a code change** but a Railway deployment configuration change.
  - Action required: Add `LINK_CODE_SECRET=<random-secret>` to the Railway API service environment variables. This value is used as an HMAC key for hashing Telegram link codes stored in Redis. Any random 32+ byte hex string works.
  - Note: This error occurs only when a user attempts to link their Telegram account. The feature is silently broken until the env var is set.
