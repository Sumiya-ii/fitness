# Test Gaps — 2026-04-29

Audit of worker processors against the 9 required test categories from CLAUDE.md.

## Legend

- PASS — category is covered
- GAP — category is missing or incomplete
- N/A — category does not apply to this processor (no delivery channels, no env var of that type)

---

## photo.processor.spec.ts

| # | Category | Status | Notes |
|---|----------|--------|-------|
| 1 | Missing env vars (OPENAI_API_KEY, GEMINI_API_KEY) | PASS | "returns empty result when no API keys are configured" |
| 2 | Happy path | PASS | Gemini + OpenAI paths both covered |
| 3 | Both delivery channels | N/A | Photo processor returns data; no notification delivery |
| 4 | No deliverable channels | N/A | See above |
| 5 | Locale handling | N/A | Locale does not affect photo parsing |
| 6 | AI fallbacks | PASS | "falls back to OpenAI when Gemini fails", "returns empty result when Gemini fails and no OpenAI key" |
| 7 | External service errors | PASS | Gemini failure + missing key covered |
| 8 | Error logging | N/A | Photo processor has no logMessage call; errors are returned not logged |
| 9 | Edge case data | PASS | Zero-value fields, confidence clamping, empty items array, missing names |

**Verdict: All applicable categories covered.**

---

## reminders.processor.spec.ts

| # | Category | Status | Notes |
|---|----------|--------|-------|
| 1 | Missing env vars (OPENAI_API_KEY, TELEGRAM_BOT_TOKEN) | GAP | TELEGRAM_BOT_TOKEN missing is tested. OPENAI_API_KEY missing is NOT tested — the processor skips AI and falls back to static, but no spec covers that code path explicitly |
| 2 | Happy path | PASS | Telegram morning + push evening + both channels |
| 3 | Both delivery channels | PASS | "sends to both channels simultaneously" |
| 4 | No deliverable channels | PASS | "skips when no deliverable channels", "skips push when pushTokens is empty array", "skips telegram when chatId is missing" |
| 5 | Locale handling | PASS | mn, en, undefined, unknown (jp) → Mongolian fallback |
| 6 | AI fallbacks | GAP | Processor uses static fallbacks when OPENAI_API_KEY is unset or GPT returns empty — no spec test for either path |
| 7 | External service errors | PASS | Telegram failure + push failure |
| 8 | Error logging | PASS | "logs failure when Telegram delivery fails", "logs failure when push delivery fails" with status: 'failed' + errorMessage |
| 9 | Edge case data | PASS | Empty pushTokens, missing chatId, null locale |

**Verdict: 2 gaps.**

### TODOs for reminders.processor.spec.ts

1. Add test: `'skips AI generation when OPENAI_API_KEY is not set, uses static fallback'`
   - `delete process.env.OPENAI_API_KEY`
   - Call with `channels: ['push'], pushTokens: ['t1'], locale: 'mn'`
   - Assert `mockSendExpoPush` was called (delivery happens) — static message used
   - Assert OpenAI mock constructor was NOT called

2. Add test: `'uses static fallback when GPT returns empty content'`
   - Requires mocking OpenAI in reminders.processor.spec.ts (currently not mocked — processor uses `new OpenAI()` only when key present)
   - Mock OpenAI to return `{ choices: [{ message: { content: null } }] }`
   - Assert delivery still happens with a non-empty static message

---

## coach-memory.processor.spec.ts

| # | Category | Status | Notes |
|---|----------|--------|-------|
| 1 | Missing env vars (OPENAI_API_KEY, DATABASE_URL) | PASS | Both covered |
| 2 | Happy path | PASS | Full aggregation + GPT + DB write |
| 3 | Both delivery channels | N/A | No notification delivery |
| 4 | No deliverable channels | N/A | See above |
| 5 | Locale handling | N/A | Locale in job data but summaries are always text; no locale-specific delivery |
| 6 | AI fallbacks | PASS | "uses fallback summaries when GPT returns partial data", "handles GPT returning empty JSON object" |
| 7 | External service errors | PASS | OpenAI error + DB aggregation error both throw + pool cleanup |
| 8 | Error logging | N/A | coach-memory uses pino logger directly, not logMessage |
| 9 | Edge case data | PASS | Empty DB results, identical weight values |

**Verdict: All applicable categories covered.**

---

## privacy.processor.spec.ts

| # | Category | Status | Notes |
|---|----------|--------|-------|
| 1 | Missing env vars (DATABASE_URL, S3_BUCKET) | PASS | Both covered |
| 2 | Happy path | PASS | Export: collect → S3 upload → presigned URL → DB update |
| 3 | Both delivery channels | PASS | Telegram + push for export URL delivery |
| 4 | No deliverable channels | PASS | "completes export without crashing when user has no tokens and no chatId" |
| 5 | Locale handling | PASS | mn, en, undefined locale for both push and Telegram |
| 6 | AI fallbacks | N/A | Privacy processor has no AI calls |
| 7 | External service errors | PASS | S3 failure throws; Telegram failure + push failure do not throw (logged) |
| 8 | Error logging | PASS | "calls logMessage with status:failed when Telegram throws", same for push |
| 9 | Edge case data | PASS | Missing fields, unknown requestType, non-pending deletion, multiple tokens, S3 key cleanup |

**Verdict: All applicable categories covered.**

---

## Summary

| Processor | Gaps | Action |
|-----------|------|--------|
| photo | 0 | Done |
| reminders | 2 (OPENAI_API_KEY env + AI fallback) | Add 2 tests (low-risk, out of scope for test-expert; assign to worker-expert) |
| coach-memory | 0 | Done |
| privacy | 0 | Done |

The 2 reminders gaps are low-risk (static fallback is simple), but they touch OpenAI mock setup that does not currently exist in that spec file. Flagged for worker-expert.
